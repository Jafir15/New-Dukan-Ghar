// Capacitor default config – will be overwritten by mode‑specific file in CI
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dukanghar', // placeholder – replaced in CI
  appName: 'Dukan Ghar', // placeholder – replaced in CI
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
