import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dukanghar.user',
  appName: 'Dukan Ghar',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
