kind = "web"
previewPath = "/"
title = "دکان گھر"
version = "1.0.0"
id = "artifacts/dukan-ghar"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19785

[services.development]
run = "pnpm --filter @workspace/dukan-ghar run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dukan-ghar", "run", "build" ]
publicDir = "artifacts/dukan-ghar/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19785"
BASE_PATH = "/"
