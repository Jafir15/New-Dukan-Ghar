import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dukanghar.admin',
  appName: 'Dukan admin',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
